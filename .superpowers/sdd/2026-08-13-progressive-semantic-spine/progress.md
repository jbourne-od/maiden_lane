# SDD ledger — plan: docs/superpowers/plans/2026-08-13-progressive-semantic-spine.md
Setup: isolated worktree `codex/progressive-semantic-spine` at planning commit `5c75f7f`; baseline `go test ./...` passed.
Task 1 clarification: no leaf encoding table existed; authorized the first narrow v1 table with exact domain tags and fixed fields under the ratified canonical rules. This is an implementation-format decision, not new team-HOS semantics.
Task 1: minor (deferred): add explicit acceptance tests that state construction does not enforce assignment equality or team member cardinality; final review must triage.
Task 1: fix round 1/5 (2 addressed, 0 open — deterministic invalid-field priority; alias-proof state/schema/world immutability tests; commits 4208d8b..fb304ee).
Task 1: complete (commits 5c75f7f..fb304ee, review clean).
Task 2: fix round 1 paused before edits — unresolved write/write conflict must fail closed, but the ratified diagnostic vocabulary has no truthful code; owner decision required before changing the canonical format contract.
Task 2: owner approved `WRITE_CONFLICT_UNRESOLVED`; design/plan amended in local commit `b9e280f`. Fix round 1 resumed.
Task 2: fix round 1/5 (4 addressed, 0 open — unresolved write conflicts; target entity access; ambiguous normalized declarations; frozen literals; commits b9e280f..4197396).
Task 2: complete (commits fb304ee..4197396, review clean; contract amendment b9e280f).
Task 3: owner approved schema-bound patches and receipt-bound inverse after review exposed unsafe destructive undo; design/plan amended in local commit `4c5cde5`. Fix round 1 started.
Task 3: fix round 1/5 (3 addressed, 0 open — receipt-authorized inverse; schema fail-closed errors; multi-field rollback; schema-bound vectors; commits 4c5cde5..0dca950).
Task 3: complete (commits 4197396..0dca950, review clean; contract amendment 4c5cde5).
Task 4 clarification: executor identity follows HLD structured `backend@<digest>` syntax (validated canonical backend token plus version digest); cross-executor exclusion uses two `go` version digests, not a speculative backend. Provenance policy remains sole `changes.v1`.
Task 4 clarification: ratified the full slice's closed ArtifactKind rank (`plan`, `compiled_profile`, `state`, `world`, `patch`, `journal_entry`, `journal_prefix`, `invariant_result_set`, `checkpoint_artifact`, `readiness_assessment`); ArtifactRef is kind plus content digest. FactRef is EntityRef plus FieldName; InvariantEvidenceRef is compiler-derived declaration key. Reference fields are private and semantic-only, never telemetry.
Task 4: structured HLD executor identity clarification recorded in design/plan commit `068ce00` (canonical backend token plus version digest; tests use two `go` version digests).
Task 4: implementation commit `51295fa`; independent review Ready=no. Fix round 1/5 opened for four confirmed findings: reject empty HOS anchors before patch materialization; execute the compiled typed output-key field; return truthful per-artifact integrity code/kind/digest evidence for journal verification failures; canonicalize invariant evidence refs independently of evaluation order.
Task 4: fix round 1/5 (4 addressed, 0 open — empty-anchor rejection; compiled output-key execution; truthful journal integrity evidence/frontier; canonical invariant refs; commits 51295fa..d8c61c2).
Task 4: complete (commits 068ce00..d8c61c2, review clean; fresh `make verify` passed).
Task 4: fix round 1/5 (4 addressed, 0 open — non-empty source/emitted anchors; compiled output-key identity; structured journal integrity classification with exact verified-prefix evidence; sorted/deduplicated invariant evidence refs).
Task 5: implementation in progress; strict RED observed for absent seal API, established-run link defects escaping as Go errors, omitted explicit checkpoint-ID manifest link, stale cached failure refs, and incorrect known-artifact conflict attribution.
Task 5: fix round 1/5 (4 review findings addressed — supplied-content artifact refs; candidate conflict attribution with known-prefix evidence; typed policy/execution link defects; full replayed checkpoint equality). Fresh verification pending.
Task 5: fix round 2/5 (2 addressed — rebuild-based cached-ID versus supplied-byte evidence; malformed policy-link typed failure). Final re-review and fresh verification pending.
Task 5: fresh `make verify` passed after fix round 2; final re-review/diff pending.
Task 5: final independent review complete (Ready=yes; full diff d8c61c2..WORKING in final-review-d8c61c2..WORKING.diff). C1/C2 checkpoint-ID/claim/manifest vectors independently re-verified by hashing the frozen literal bytes outside the production encoder; claim fields confirmed present in manifest; executor identity confirmed absent. Semantic suite + race pass in reviewer sandbox (Go 1.24.7 — pinned 1.26.5 unavailable there; workstation make verify remains the authoritative pass).
Task 1 deferred minor triaged: partially addressed — TestNewStateDoesNotEnforceTeamHOSRuleSemantics covers HOS-value semantics and empty assignment presence; explicit construction-acceptance tests for cross-driver assignment inequality and team member-cardinality remain open (minor, non-blocking).
Task 6: implemented by fresh subagent (profile.go, profile_test.go, canonical.go assessment encoders); root-agent independent review clean (Ready=yes). Link verification precedes evaluation; established-run defects are typed integrity outcomes (ARTIFACT_DIGEST_MISMATCH / ARTIFACT_LINK_INCONSISTENT / ASSESSMENT_IDENTITY_CONFLICT), malformed non-established inputs are Go errors. Ready and needs_input canonical vectors independently re-verified by hand-hashing the frozen literal bytes; optimizer=>CM implication proved bounded-exhaustively (16 single + 256 paired presence states) through the public Assess API; non-omission, immutability, and mutate-nothing tests present. Semantic suite + race pass in reviewer sandbox (Go 1.24.7; pinned-toolchain gate remains CI and workstation make verify).
Task 6 implementation decisions: assessment-semantics version token maiden-lane.assessment-semantics.v1 encoded after the domain tag; missing requirement results carry zero fact refs; trailing assessment-level evidence count always zero in v1 per ratified record shape; satisfied/missing is a one-byte closed union (1/2).
Task 7: implemented by fresh subagent (internal/fixtures/teamhos: fixture.go data-only + fixture_test.go); root-agent independent review clean (Ready=yes). Every ratified literal cross-checked by the root agent directly against design sections 4.2/10.1/10.2 (lineage descriptor, A/B source keys, assignment X, tuples (T0,10,8)/(T0,7,6) and mismatch (T1,7,6), empty world, changes.v1). Direct-kernel lifecycle proved through public semantic APIs for both variants: passing reaches C2 ready/ready with aggregate (T0,10,8) and accepted journal [form_team.v1, aggregate_team_hos.v1]; anchor mismatch rejects with HOS_ANCHOR_MISMATCH pre-patch, C1 re-seals byte-identical, no C2. Non-import of the fixture by production packages pinned by test. Suites + race pass in reviewer sandbox (Go 1.24.7; authoritative gate remains CI/workstation make verify). Executor identity and compiler-semantics version literals match the in-package frozen golden vectors by design.
Task 8: implemented by fresh subagent (internal/app: observation/result/errors/progressive + two test files); root-agent independent review clean (Ready=yes). Run orchestrates compile->bind->transitions->seal->assess generically over the compiled plan (no fixture import in production; pinned by test). Verified frontier is dependency-closed; exclusion on assess-integrity uses the report's own LastVerifiedCheckpointArtifactID. Machinery vs semantic split: typed rejection => nil Go error + retained prefix; machinery => non-nil error with fixed safe text, cause chain preserved for errors.Is/As; observer panics recovered and non-authoritative; identical SpineResult bytes across nil/discard/recording/panicking observers. Suites + race pass (reviewer sandbox Go 1.24.7; authoritative gate remains CI/workstation).
Task 8 OWNER DECISION PENDING: the plan's fixed interface names SpineResult.ExecutionStatus() (semantic.ExecutionStatus, bool), but no such type exists in internal/semantic (Tasks 1-7 never needed it) and Task 8's file list forbids touching semantic. Implemented as app-owned ExecutionStatus (pending/running/succeeded/failed per HLD lifecycle; design section ~968 lists it as a distinct named type without assigning a package). Recommendation: ratify app ownership (execution lifecycle is application/control-plane vocabulary; it never enters canonical semantic identity per Inviolate 2) and amend the plan text. Merging the PR constitutes approval; otherwise relocate before merge. SpineStatus follows the ratified design tokens succeeded/failed/invalid_plan, superseding the plan sketch's unnamed variants.
Task 8 disclosed gaps (minor): the assess-exclusion branch for a checkpoint whose own content identity fails re-verification is implemented but not test-reachable without in-package corruption helpers; the aggregate branch of proposedOperationCounts is untested because a rejected Update is unreachable through the public kernel. Both guarded by mutation checks; candidates for Task 10's matrix.
Task 8 owner decision RATIFIED (Jake, 2026-08-14): app-owned ExecutionStatus approved as a plan amendment, Task 2 precedent. Ratified rationale, preserved verbatim in the type's doc comment so a future cleanup cannot lovingly reintroduce the design error: ExecutionStatus is application-owned control-plane state and is excluded from canonical semantic identity and artifact encoding. Semantic layer answers what the computation meant; application/control plane answers what happened while it ran. Invariant: equivalent semantic executions remain equivalent regardless of application lifecycle representation — new lifecycle vocabulary (queued/retrying/timed_out/cancelled) must never force a semantic schema/version change, and new semantic outcomes must never require the execution controller to own semantic vocabulary. The Task 10 documentation pass carries this sentence into the implementation guide. Disclosed test gaps confirmed as enumerated, bounded, and assigned to the Task 10 matrix (deferred verification, not hidden debt).
Task 9: implemented by the root agent in Claude Code (internal/observability: semantic.go adapter, semantic_dimensions.go closed vocabulary, semantic_metrics.go contract, semantic_test.go; runtime.go/runtime_test.go registration). No subagent was used: AGENTS.md section 22 prefers delegation, but the owner's handover directed a single CLI session to carry the spine and this task is one indivisible file set in one package with no independently reviewable split. Import direction verified both ways: `go list -deps ./internal/semantic` shows only the nine standard-library packages Task 1 authorized (os appears solely transitively via fmt/crypto), and `go list -deps ./internal/app` contains no OTel or observability. SemanticObserver has no non-test caller; no CLI/HTTP surface was wired. Fresh `make verify` passed (fmt, tidy, tool versions, vet, Staticcheck, tests, race, govulncheck, build).
Task 9 implementation decisions: (1) observability re-declares the complete closed vocabulary (observationPhase/observationResult/operationKind/operationResult/checkpointResult/profileKind/readinessVerdict/transitionKind/checkpointKind/closedCode) and maps app values through exhaustive switches that repeat the tokens rather than delegating to app's String(), so widening the app vocabulary cannot widen telemetry without an edit in this package; closedCode's field is unexported so only a mapper can produce a code dimension. (2) Ratified metric dimension keys stay unprefixed per design 11.4; span attribute keys are namespaced `maiden_lane.semantic.*` because one trace mixes HTTP and semantic spans and bare `phase`/`result` would collide. (3) Bounded counts are carried as span attributes (admitted by design 11.3) only when non-zero. (4) Metric recording consumes app's MetricProjection(), never the observation, so the three observed identities are structurally unreachable from metrics.
Task 9 owner decision RATIFIED (Jake, 2026-08-15): omission for optional dimensions approved as recommended, Task 8 precedent. Ratified rationale, preserved verbatim in the semantic_dimensions.go header comment so a later cleanup cannot lovingly make the two cases uniform: optional bounded dimensions fail closed by omission, not by substitution. A required dimension must always exist — a span needs a name and a status, and a completed phase needs a duration point — so an unadmitted phase or result maps to internal_error; that value is deliberately outside design 11.4's enumerated phase list and is a tripwire, not a category. An optional dimension already has an honest absent representation, so an unadmitted value is omitted rather than labeled: emitting profile_kind=internal_error would place a value outside the ratified exact-value list into a bounded vocabulary and would assert a classification the spine never made. Invariant: telemetry may drop a point it cannot truthfully label, but it may never invent or widen a bounded dimension value. Both halves are pinned by TestSemanticOptionalDimensionsFailClosedByOmission — an unadmitted profile records no assessment point while the phase duration survives, and a zero-valued carrier still yields a named, explicitly failed internal_error span plus its duration point. The Task 10 documentation pass carries this rule into METRICS.md.
Task 9 plan deviations (2, both disclosed): (a) the implementation was split into semantic.go/semantic_dimensions.go/semantic_metrics.go rather than the single planned semantic.go, because one file reached 609 non-comment lines against the plan's 350-line split rule; the three files are 156/297/168 and split on the plan's own responsibility seams. (b) Task 7's TestProductionPackagesDoNotImportFixture walked _test.go files, which contradicts Task 9's requirement to keep real end-to-end app.Run coverage in internal/observability. The walk now skips _test.go and still covers every production file in cmd, internal/httpapi, and internal/observability; the test's own name and doc comment state the invariant as production binaries, and Go never links _test.go into a binary. The stronger `go list -deps` proof belongs to Task 10 step 1.
Task 9 coverage: end-to-end app.Run through the real adapter for passing, anchor-mismatch, and invalid-plan requests (span names/counts/parenting/status, exact metric points, no UNSET status, closed-value assertion over every span attribute). Split-half coverage for outcomes the public request cannot produce: exhaustive phase/result/kind/code mapping including span status per result and the invariant-code subset separation, plus metric recording driven from observability-owned normalized dimensions for rejected operations and a refused seal. Noninterference proved by byte-identical SpineResult projections across nil, discard, recording, asynchronously exporter-failing, and disabled-runtime observers. Concurrency: four simultaneous runs through one adapter yield four well-formed 10-span traces with zero retained stacks; race detector clean. No observation constructor or production test hook was exported.
Task 10: implemented by the root agent in Claude Code (internal/semantic/boundary_test.go, internal/fixtures/teamhos/matrix_test.go, README.md, METRICS.md, ERRORS.md, docs/implementation/implementation-guide.md). Step 2 was run before any documentation edit: the four-package suite and the race suite both passed first, so no current-state doc claims behavior that was not already green. Fresh `make verify` and `make container-check` both passed on the workstation with Docker available, so no verification stage was skipped or recorded as unavailable.
Task 10 step 1 coverage: (a) TestSemanticKernelImportsStayPureAndClosed parses every file in internal/semantic with go/parser and enforces an explicit local ALLOWLIST (bytes, crypto/sha256, encoding/binary, encoding/hex, fmt, slices, sort, strings, unicode/utf8; plus testing for _test.go) rather than a denylist, so an unreviewed import fails closed instead of being admitted by omission; a redundant forbidden-prefix denylist covers app/observability/fixtures/httpapi/OTel/AWS/stochflow/log/net/os/time/rand/uuid/sql/json/reflect as defense in depth. Negatively verified: a probe file importing time was rejected on both the allowlist and denylist checks before removal. TestSemanticImportAllowlistHasNoUnusedEntries prevents the allowlist drifting ahead of the code. (b) TestLifecycleIdentityIgnoresCombinedAuthoringOrder drives the complete passing lifecycle twice, once with schema entity/field/relation declarations, rules, checkpoints, declared reads/writes, the explicit source pair, profiles, requirement atoms, and S0 entities/relations all reversed, and requires equality across plan ID, both profile IDs, world/input/run/execution IDs, all three state digests, both patch digests, both journal prefix digests, both checkpoint IDs and digests, all four assessment IDs and digests, and all four verdicts. (c) TestLifecycleIdentitySensitivityMatrix asserts the opposite direction per dimension (lineage, observation, world, plan, profile), plus the derived invariants that a moved checkpoint ID must move its content digest and that AssessmentID moves exactly when its checkpoint or profile does. Executor exclusion and attempt absence are separate named tests.
Task 10 disclosed limitations (2, both documented in the implementation guide's Known gaps rather than silently omitted): (a) the plan's identity matrix names a provenance-policy dimension, but ProvenancePolicy is a closed union whose only ratified value is changes.v1, so there is no second lawful value to vary; the policy participates in run/checkpoint identity by construction and BindRun rejects invalid values. A row is deliberately absent with an in-test comment explaining why, to be added when a second policy is ratified. (b) AttemptID does not exist anywhere in the implemented surface, so its exclusion from canonical identity is vacuous; TestNoAttemptConceptExistsInTheSemanticSurface pins the absence and proves identical inputs reproduce every identity including ExecutionID.
Task 10 boundary-checker exemption: boundary_test.go must read the package directory and parse Go source, so it necessarily imports os, path/filepath, go/parser, and go/token and cannot check itself. The exemption is exactly one named file and TestSemanticBoundaryExemptionIsExactlyThisFile asserts both that the file exists and that its own imports stay within the seven that checking requires, so the hole cannot widen.
Task 10 documentation: README no longer claims the transformation engine is unimplemented; it describes the internal pure walking skeleton and states in bold that there is still no public transformation API, route, CLI, persistence, promotion, publication, or production HOS policy, and that the sanitized fixture is not a real rule. METRICS.md registers all five semantic instruments with exact types, units, dimension allowlists, permitted values, and the ratified recording rules, and states that the runtime registers them while the production process records no points. ERRORS.md replaces its reserved status with the two Task 8 machinery error types and their three codes, and adds an explicit section separating semantic results (typed values, nil Go error, never retryable, not cataloged) from machinery errors (the only Go errors, the only entries); its publication column states which artifacts survive rather than describing a gate that does not exist. The implementation guide's capabilities, repository map, semantic spine flow, Go orientation, and gaps were rewritten to match the actual files, and it now carries both ratified sentences: the Task 8 ExecutionStatus control-plane split and the Task 9 optional-dimension omission rule. Every doc claim was traced to a passing test before it was written.
Task 10 step 7 deviation: the plan directs dispatching at least two read-only reviewers. No subagent was dispatched. The owner's handover moved this work to a single CLI session and explicitly offered to act as the out-of-band diff reviewer, and this session is configured not to spawn agents unless asked. The root agent instead ran both prescribed passes itself: the invariant/identity pass traced every new documentation claim to a specific passing test (failed-suffix C1 byte-identity, vacuous-empty readiness, non-omission of a second incomplete team) and re-ran the constitutional boundary inspection, and the observability/security pass re-confirmed one-way import direction by dependency closure, result noninterference, the trace allowlist, bounded metric cardinality (well under the 2000 limit), safe fixed error text, and lifecycle shutdown. Owner review of the PR diff is the intended independent pass.
